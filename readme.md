To connect project to MongoDb - add nodemon.json file to root folder with yout credentials:

{
    "env": {
        "MONGO_ATLAS_USER": "*****",
        "MONGO_ATLAS_PW": "*****",
        "JWT_KEY": "*****"
    }
}
